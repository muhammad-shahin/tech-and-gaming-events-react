import React from 'react';
import PropTypes from 'prop-types';

const ShowBookedEvents = props => {
    return (
        <div>
            
        </div>
    );
};

ShowBookedEvents.propTypes = {
    
};

export default ShowBookedEvents;